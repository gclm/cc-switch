interface:
  display_name: "Cc Switch"
  short_description: "Repo-specific patterns and workflows for cc-switch"
  default_prompt: "Use the cc-switch repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true